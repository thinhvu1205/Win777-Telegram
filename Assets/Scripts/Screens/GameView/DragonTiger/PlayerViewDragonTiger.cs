using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerViewDragonTiger : PlayerView
{
    // Start is called before the first frame update
    private void Start()
    {
        //transform.localScale = new Vector2(0.7f, 0.7f);
    }
}
